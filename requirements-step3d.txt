# Optional: Step 3D tab (PyVista + VTK in Qt). Not required for core BOM/PnP.
# From the boomer project root (e.g. ~/cursor/boomer): activate .venv or venv, then:
#   pip install -r requirements-step3d.txt
pyvista>=0.43,<0.50
pyvistaqt>=0.11
vtk
